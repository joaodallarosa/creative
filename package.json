{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "nuxt generate && push-dir --dir=dist --branch=master --cleanup"
  },
  "devDependencies": {
    "nuxt": "npm:nuxt3@latest",
    "push-dir": "^0.4.1",
    "tailwindcss": "^3.0.7"
  },
  "dependencies": {
    "@heroicons/vue": "^1.0.5",
    "p5": "^1.4.1",
    "p5i": "^0.4.2"
  }
}
